# Akeah Diez | Enchanted IT Portfolio

A responsive personal portfolio website for Akeah Diez, a BSIT student and aspiring IT professional. Built with vanilla HTML, CSS, and JavaScript, this portfolio showcases web development skills with a beautiful, fairytale-inspired visual design.

## Project Overview

This portfolio fulfills all requirements of the **Web Development Intern Challenge** from the Skills Showcase Challenge by New Technologies Inc., Cebu City. It features three main sections (Home, Projects/Work, and Contact) with smooth navigation, responsive design, and interactive elements.

## ✨ Features

**Core Requirements:**
- ✅ **Home Section** - Name, profile picture, and short bio with a beautiful banner
- ✅ **Projects Section** - List of 3 real projects with titles, descriptions, tech stack, and images
- ✅ **Contact Section** - Functional contact form (name, email, message) with success alert on submit
- ✅ **Responsive Design** - Optimized for mobile, tablet, and desktop devices
- ✅ **Smooth Navigation** - Sticky navigation with active section highlighting
- ✅ **Clean Layout** - Uses CSS Grid and Flexbox for modern, organized design

**Additional Features:**
- Animated elements (lanterns, stars, scroll fade-ins, hover effects)
- Project filtering by category (Full stack, Frontend)
- Resume view and download functionality
- Accessible semantic HTML with ARIA labels
- CSS-based decorative artwork (fallback if images missing)
- Smooth scroll behavior
- Glass-morphism navigation header
- Mobile-friendly hamburger menu

## 🛠️ Tech Stack

- **Frontend:** HTML5, CSS3, Vanilla JavaScript
- **Fonts:** Google Fonts (Cormorant Garamond, DM Sans)
- **Responsive:** Mobile-first with media queries
- **No dependencies** - Pure vanilla code, no frameworks or build tools required

## 📁 Folder Structure

```
diez-portfolio/
├── index.html                    # Main HTML file
├── style.css                     # Main stylesheet
├── script.js                     # JavaScript functionality
├── README.md                     # This file
├── editorial.css                 # Alternative stylesheet (legacy)
├── editorial-v2.css              # Alternative stylesheet (legacy)
├── assets/
│   ├── images/
│   │   ├── profile.jpg          # Profile picture
│   │   └── rapunzel.png         # Optional decorative artwork
│   └── Akeah_Diez_Resume.pdf    # Resume file
└── .vscode/                      # VS Code settings (optional)
```

## 🚀 How to Run

### Option 1: Direct Browser Open
1. Navigate to the project folder on your computer
2. Double-click `index.html` to open in your default browser
3. The site will load immediately with all features working

### Option 2: VS Code with Live Server
1. Open the project folder in VS Code
2. Install the **Live Server** extension (if not already installed)
3. Right-click on `index.html` and select **Open with Live Server**
4. Your default browser will open with auto-refresh enabled

### Option 3: File System
1. Open File Explorer and navigate to the project folder
2. Right-click `index.html` → Open with → Choose your browser
3. The website will load with all interactivity

## 📋 Requirements Checklist

### Must Have ✅
- [x] Built using HTML + CSS + JavaScript (vanilla, no frameworks)
- [x] Responsive design (mobile, tablet, desktop)
- [x] Clean, organized layout using Flexbox/Grid
- [x] Smooth navigation between sections
- [x] All three required sections (Home, Projects, Contact)
- [x] Functional contact form with success alert
- [x] Accessible with semantic HTML

### Bonus Features 🌟
- [x] Scroll fade-in animations
- [x] Hover effects and transitions
- [x] Project filtering system
- [x] Glass-morphism navigation header
- [ ] Dark mode toggle (optional enhancement)
- [ ] GitHub API integration for dynamic repos (optional enhancement)
- [ ] Deployed online (ready to deploy to GitHub Pages or Vercel)

## 🎨 Design System

**Color Palette:**
- Primary Background: `#f4f0e8` (soft cream)
- Dark Background: `#111` (deep black)
- Accent Color: `#6f1d2b` (cherry red)
- Secondary Accent: `#d7a6a9` (dusty rose)

**Typography:**
- Serif: Cormorant Garamond (headings)
- Sans-serif: DM Sans (body text)
- Smooth easing functions for animations

## 🔧 Form Functionality

The contact form:
- Collects name, email, and message
- Validates that all fields are filled
- Shows a personalized success alert upon submission
- Resets form after successful submission
- Client-side only (no server required)

## 📱 Responsive Breakpoints

- **Desktop:** 1240px and above
- **Tablet:** 900px to 1240px
- **Mobile:** 560px to 900px
- **Small Mobile:** Below 560px

## ♿ Accessibility

- Semantic HTML structure (header, nav, main, section, footer)
- ARIA labels and descriptions
- Skip to main content link
- Keyboard navigation support
- Reduced motion support for users with motion sensitivity

## 📝 Customization Guide

### Update Your Information
1. **Name:** Search for "AKEAH DIEZ" in `index.html` and replace
2. **Bio:** Update the hero section text
3. **Profile Picture:** Replace `assets/images/profile.jpg`
4. **Email:** Update the mailto link in the contact section
5. **GitHub:** Update your GitHub profile URL
6. **LinkedIn:** Update the LinkedIn profile URL
7. **Resume:** Replace `assets/Akeah_Diez_Resume.pdf`

### Update Projects
Each project card has:
- Project number and category
- Title and description
- Technologies used
- GitHub and Demo links

Edit these in the Projects section of `index.html`.

## 🔄 Browser Support

- Chrome (latest)
- Firefox (latest)
- Safari (latest)
- Edge (latest)
- Mobile browsers (iOS Safari, Chrome Mobile)

## 📦 Deployment Options

### GitHub Pages (Free)
1. Create a GitHub repository
2. Push all files to the repo
3. Go to Settings → Pages
4. Select "Deploy from a branch"
5. Choose `main` branch and `/ (root)` folder
6. Your site will be live at `https://username.github.io/repo-name`

### Vercel (Free)
1. Push code to GitHub
2. Go to [vercel.com](https://vercel.com)
3. Import your repository
4. Click Deploy
5. Your site will be live instantly with auto-updates

### Traditional Hosting
Simply upload all files to any web host via FTP or file manager.

## 📄 Challenge Requirements Met

✅ **Submission Requirements:**
- [x] All source code (HTML/CSS/JS) included
- [x] README.md with project description
- [x] Technologies used listed
- [x] Setup instructions provided
- [x] Portfolio is fully functional
- [x] Clean and visually appealing design
- [x] Responsive on mobile and desktop
- [x] At least 3 sections (Home, Work/Projects, Contact)
- [x] Functional contact form with success alert
- [x] Ready for GitHub repository submission

## 👤 About the Developer

**Akeah Jailyn Diez**
- Bachelor of Science in Information Technology student at University of San Carlos
- Interested in web development, UI/UX design, and software development
- Seeking OJT opportunity to gain industry experience

## 📞 Contact

- **Email:** akeahjailyndiez@gmail.com
- **GitHub:** [akeahjailyndiez](https://github.com/akeahjailyndiez)
- **LinkedIn:** [Akeah Jailyn Diez](https://www.linkedin.com/)

## 📜 License

This project is open-source and available for personal and educational use.

---

**Made with curiosity, courage, and a little moonlight.** ✨
4. Google Fonts load when you have an internet connection. The site uses fallback fonts if they cannot load.

## Personalize the portfolio

### Change your name and information

Open `index.html` and search for the text you want to change. Your name appears in the navigation, hero, and footer. The About, Education, OJT, and Contact sections are also regular HTML text, so you can edit them directly.

### Add your profile or fairytale images

Only add images that you created or have permission to use. Put them inside `assets/images/` with these names:

- `rapunzel.png`
- `tower.png`
- `lanterns.png`

The hero is already prepared for `rapunzel.png`. The CSS illustration remains visible when the file is missing. You can add the other images later by placing an `<img>` element in the section where you want it and using a path like `assets/images/tower.png`.

### Add your resume

Place your actual PDF in `assets/` and name it exactly `Akeah_Diez_Resume.pdf`. Both resume buttons already point to that path. The comments next to the buttons mark the edit area.

### Change project details and links

Each project is an `<article>` in the Projects section of `index.html`. Edit its name, type, description, role, technologies, and links. Search for `Replace both placeholder URLs` to find the three link areas. Replace the placeholder GitHub URL and the `#contact` Live Demo URL with your real URLs.

### Add contact links

In the Contact section, replace `your.email@example.com`, `github.com/yourusername`, and `linkedin.com/in/yourusername` with your own details. Update both the visible text and the `href` value. For email, keep the `mailto:` prefix.

## Beginner Git and GitHub guide

### 1. Create a GitHub repository

1. Sign in at [github.com](https://github.com/).
2. Click the **+** button, then **New repository**.
3. Name it something like `akeah-portfolio`.
4. Choose **Public** if you want GitHub Pages to show it publicly.
5. Do not add another README when GitHub asks, because this folder already has one.
6. Click **Create repository**.

### 2. Connect this folder to GitHub

Open the VS Code terminal with **Terminal > New Terminal**. Run these commands one at a time:

```bash
git init
git add .
git commit -m "Initial portfolio"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/akeah-portfolio.git
git push -u origin main
```

What each command does:

- `git init` starts Git tracking in this folder by creating a hidden `.git` folder.
- `git add .` stages every new and changed file for the next save point. The dot means “this folder.”
- `git commit -m "Initial portfolio"` saves the staged files into Git history with a message.
- `git branch -M main` names the current branch `main`, which is the common default branch name.
- `git remote add origin ...` gives your local folder the web address of your GitHub repository. Replace the example URL with your own.
- `git push -u origin main` uploads your local `main` branch to GitHub and remembers that destination for future pushes.

GitHub may ask you to sign in. Follow the sign-in window or browser prompt. Never put a password or access token inside this README.

### 3. Update the website later

After changing files, use:

```bash
git add .
git commit -m "Update portfolio content"
git push
```

This stages your edits, records them, and uploads them. The next GitHub Pages build will use the latest version.

### 4. Turn on GitHub Pages

1. Open your repository on GitHub.
2. Click **Settings**.
3. In the left menu, click **Pages**.
4. Under **Build and deployment**, choose **Deploy from a branch**.
5. Choose branch **main** and folder **/ (root)**.
6. Click **Save**.
7. Wait a few minutes and refresh the Pages screen. GitHub will show your portfolio URL, usually:
   `https://YOUR-USERNAME.github.io/akeah-portfolio/`

Your repository name and GitHub username determine the final URL.

## Before publishing checklist

- Replace the email, GitHub, and LinkedIn placeholders.
- Replace project GitHub and Live Demo links.
- Add your permitted images to `assets/images/`.
- Add `assets/Akeah_Diez_Resume.pdf`.
- Open the page on a phone-sized browser window and test the menu.
- Test every button and link after replacing its placeholder.
